
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top">
        <div class="container nav-container">
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/saved">Saved Articles</a>
                    </li>
                    <li class="nav-item">
                        <form action="/scrape" method="POST" class="navbar-form navbar-left">
                            <button type="submit" class="nav-link" href="/scrape">Scrape New Articles</a>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

<div class="jumbotron">
    <h1 class="text-center">Music News</h1>
</div>

<div class="container">

    {{#each articles}}
        <div class="row">
            <div class="col-sm-12">
                <h4>{{this.headline}}</h4>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-2">
                <img class="img-fluid" src="{{this.image}}" alt="">
            </div>

            <div class="col-sm-10">
                {{this.summary}}
            </div>
        </div>

        <div class="row">
            <div class="col-sm-6">
                <a href="{{this.url}}" target="_blank">Read Here</a>
            </div>
            <div class="col-sm-6">
                <button type="button" class="btn btn-default save-button" data={{this._id}}>Save Article</button>
            </div>
        </div>

    {{/each}}

</div>
